return {
    {
      "wuelnerdotexe/vim-astro",
      ft = { "astro" }, -- loads this plugin only for Astro files
      config = function()
        -- Additional configuration for vim-astro can go here if needed.
        -- For example, you can set custom syntax options.
      end,
    },
  }
  